[WSGI]
ModulePath = app.py # Path to WSGI application 
ApplicationName = app # Name of the calleble function that implements the WSGI protocol

[SERVER]
Host = localhost # WSGI (application) server host
Port = 8000 # # WSGI (application) server port